@model BlogApplication.Models.Comment

@{
    ViewData["Title"] = "Details";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog App - Comment Details</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f8f9fa;
        }

        .content {
            padding: 2rem;
        }

        .table {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

            .table thead th {
                background-color: #343a40;
                color: #fff;
                font-weight: bold;
                border-color: #454d55;
                padding: 1rem;
            }

            .table td {
                border-color: #dee2e6;
                padding: 1rem;
            }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

            .btn-primary:hover {
                background-color: #0056b3;
                border-color: #0056b3;
            }

        .btn-info {
            background-color: #17a2b8;
            border-color: #17a2b8;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

            .btn-info:hover {
                background-color: #138496;
                border-color: #138496;
            }

        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
            transition: background-color 0.3s ease, border-color 0.3s ease;
        }

            .btn-danger:hover {
                background-color: #c82333;
                border-color: #c82333;
            }
    </style>
</head>
<body>
    <div class="content">
        <h1 class="display-4 mb-4"><span style="font-style: italic;">Comment Details</span></h1>
        <div class="table-responsive">
            <table id="CommentTable" class="table table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th><span style="font-style: italic;">Content</span></th>
                        <th><span style="font-style: italic;">Post</span></th>
                        <th><span style="font-style: italic;">Author</span></th>
                        <th><span style="font-style: italic;">Date of creation</span></th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><span style="font-style: italic;">@Html.DisplayFor(model => model.Content)</span></td>
                        <td><span style="font-style: italic;">@Html.DisplayFor(model => model.Post.Title)</span></td>
                        <td><span style="font-style: italic;">@Html.DisplayFor(model => model.Author.Username)</span></td>
                        <td><span style="font-style: italic;">@Html.DisplayFor(model => model.CreatedAt)</span></td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div>
            @if (ViewBag.role == "Admin")
            {
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.CommentId">Edit</a>
            }
            <a class="btn btn-sm btn-secondary" asp-controller="Comment" asp-action="Index" asp-route-postId="@ViewBag.PostId"><span style="font-style: italic;">Back To List</span></a>
        </div>
    </div>
</body>
</html>